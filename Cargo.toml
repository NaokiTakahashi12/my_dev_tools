[package]
name = "my_dev_tools"
version = "0.1.0"
edition = "2024"

[features]
default = []
plot = ["dep:ratatui", "dep:crossterm"]

[dependencies]
crossterm = { version = "0.29", optional = true }
csv = "1.4"
ratatui = { version = "0.30", default-features = false, features = ["crossterm"], optional = true }
